syntax = "proto3";

option go_package = "pkg/orderProto";

message Info{
    string OrderID  = 1;
    string TaxiType  = 2;
}

message Driver {
    string ID = 1;
	string Name = 2;
	string PhoneNumber = 3;
	float Raiting = 4;
}

service OrderService{
    rpc FindDriver(Info) returns (Driver) {}
}